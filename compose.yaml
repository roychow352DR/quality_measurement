services:
  website:
    build: .
    env_file:
      - path: ${JIRA_ENV_FILE:-./.env.jira}
        required: false
    ports:
      - "127.0.0.1:${WEB_PORT:-3000}:3000"
    restart: unless-stopped
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
